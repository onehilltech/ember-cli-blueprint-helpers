{
  "name": "ember-cli-blueprint-helpers",
  "version": "0.2.1",
  "description": "Helper entities for usage in ember-cli Blueprint models",
  "main": "./lib",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "npm publish",
    "postpublish": "git push origin master & git push origin --tags"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "bluebird": "^3.5.1",
    "core-object": "^3.1.5",
    "npm-check": "^5.8.0"
  }
}
